Added EditorPanel component with textarea, Run/Save/New/Close toolbar, and glyph preview. Demos and uploads now open in editor instead of immediately executing. Edit button in control bar opens empty editor for writing from scratch. Dirty indicator warns before discarding changes. Full Catppuccin Mocha styling. Verified end-to-end: load demo → edit → run → REPL output, and new file → write code → run.